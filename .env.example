# GitHub API Configuration
GITHUB_TOKEN=your_github_token_here      # Personal access token with repo scope
GITHUB_OWNER=your_org_or_username        # GitHub organization or username
GITHUB_REPO=your_repository_name         # Repository name

# Example values:
# GITHUB_TOKEN=ghp_1234567890abcdef...   # Get from GitHub Settings > Developer settings > Personal access tokens
# GITHUB_OWNER=paddock-org               # Your GitHub username or organization name
# GITHUB_REPO=horse-paddock              # The name of your repository

# Deployment Mode
VITE_SERVERLESS='false'                  # Set to 'true' for serverless deployment (e.g., GitHub Pages)
                                        # Note: Must be quoted string. Restart dev server after changing.

# Game Server (only needed if VITE_SERVERLESS is 'false')
VITE_GAME_SERVER_URL=http://localhost:3131

# GitHub Project (only needed if VITE_SERVERLESS is 'false')
VITE_APP_GITHUB_PROJECT_NUMBER=1         # Find your project number at:
                                        # https://github.com/orgs/{GITHUB_OWNER}/projects
                                        # or
                                        # https://github.com/{GITHUB_OWNER}/{GITHUB_REPO}/projects